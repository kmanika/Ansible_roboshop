- name : Demo var
  hosts: all
  vars:
    URL: google.com
  tasks:
    - name: print variable
      debug:
        msg: "URL = {{URL}}"
    - name: task level var
      vars:
       URL: task.google.com
      debug:
        msg: "task URL = {{URL}}"
